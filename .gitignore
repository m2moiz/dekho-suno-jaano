# scratch/ holds working DATA -- recordings, transcripts, gate run dirs. That is
# what is ignored. The scripts in it are code and are tracked: resume_gate.py and
# diarize_gate.py are the only checks that catch a silently-restarting resume or
# a skipped diarization pass, and an untracked check is a check nobody runs.
scratch/*
!scratch/*.py
!scratch/*.sh
# One level down too. `scratch/*` stops git descending into a subdirectory at
# all, so a probe filed under scratch/vlm/ was silently untrackable -- the
# exact "an untracked check is a check nobody runs" failure this block exists
# to prevent, reappearing one directory deeper. Re-ignore the contents, then
# carve the code back out.
!scratch/*/
scratch/*/*
!scratch/*/*.py
!scratch/*/*.sh

.venv/
__pycache__/
# Build output, produced by `uv build`. Source is tracked; artifacts are not.
dist/
*.wav
*.mov

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Planning docs stay local (global pre-commit hook denies AI tooling files)
.planning/

# Coverage output and pytest caches are artifacts, not source.
.coverage

# Machine-local git hooks. core.hooksPath points here, so they are LIVE --
# but they hardcode absolute interpreter paths for this machine and named
# another project on disk. They are not source; they do not belong in a repo.
.beads/hooks/

# mutmut writes its mutants and results here.
mutants/
.mutmut-cache

# Machine-local tool output. .codegraph is an ABSOLUTE symlink into ~/.omo:
# committing it, or letting a build sweep it in, produces an archive that
# cannot be unpacked anywhere but this laptop. graphify-out/ is generated.
.codegraph
graphify-out/
.audit/

# Machine-local server state, not project code. config.yaml at the repo root
# configures a Dolt server that is LIVE on port 3307 (PID confirmed listening
# on 2026-09-22), so it must not be deleted -- but a port number is this
# laptop's setting, not the project's. .beads.gate.lock is a zero-byte lock
# written by a gate hook and referenced by nothing in this repository.
/config.yaml
.beads.gate.lock
